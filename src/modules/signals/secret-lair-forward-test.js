import { rest } from '../../core/rest.js';
const LIVE_NAME='Secret Lair: A Perfectly Normal Superdrop';
const esc=s=>String(s??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]));
const money=n=>Number.isFinite(Number(n))&&Number(n)>0?`$${Number(n).toFixed(2)}`:'—';
const pct=n=>Number.isFinite(Number(n))?`${Math.round(Number(n))}%`:'—';
function style(){if(document.getElementById('cxSlForwardStyle'))return;const s=document.createElement('style');s.id='cxSlForwardStyle';s.textContent=`.cx-sl-forward{margin-top:7px;border-top:1px solid var(--color-border);padding-top:7px}.cx-sl-forward summary{font-size:8px;font-weight:900;text-transform:uppercase;letter-spacing:.05em;color:var(--color-text-secondary);cursor:pointer}.cx-sl-ftgrid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:7px}.cx-sl-ft{border:1px solid var(--color-border);border-radius:8px;padding:7px;background:var(--color-bg-primary)}.cx-sl-fttop{display:flex;justify-content:space-between;gap:6px;align-items:center}.cx-sl-fttop b{font-size:8px}.cx-sl-ftstate{font-size:7.5px;color:var(--color-accent);font-weight:900}.cx-sl-ftmetrics{display:grid;grid-template-columns:repeat(4,1fr);gap:5px;margin-top:6px}.cx-sl-ftmetric span{display:block;font-size:6.8px;color:var(--color-text-secondary)}.cx-sl-ftmetric b{font-size:8.5px}.cx-sl-ftbar{height:28px;margin-top:6px;display:flex;align-items:end;gap:1px}.cx-sl-ftbar i{flex:1;min-width:2px;background:var(--color-border);border-radius:2px 2px 0 0}.cx-sl-ftbar i[data-live='1']{background:var(--color-accent)}.cx-sl-ftnote{font-size:7px;color:var(--color-text-secondary);margin-top:5px}@media(max-width:700px){.cx-sl-ftgrid{grid-template-columns:1fr}.cx-sl-ftmetrics{grid-template-columns:repeat(2,1fr)}}`;document.head.appendChild(s)}
function article(name){return [...document.querySelectorAll('#cxSecretLairSignals .cx-sl-drop')].find(a=>a.querySelector('.cx-sl-title strong')?.textContent?.trim()===name)}
function bars(rows){const vals=(rows||[]).map(r=>Number(r.listing_count||0));const max=Math.max(1,...vals);return `<div class="cx-sl-ftbar">${vals.slice(-24).map((v,i,a)=>`<i data-live="${i===a.length-1?1:0}" style="height:${Math.max(2,Math.round(v/max*28))}px" title="${v} listings"></i>`).join('')}</div>`}
function card(label,o,rows){o=o||{};return `<div class="cx-sl-ft"><div class="cx-sl-fttop"><b>${esc(label)} · frozen ${esc(o.frozen_recommendation||'—')}</b><span class="cx-sl-ftstate">${esc(String(o.outcome_status||'observing').replaceAll('_',' '))}</span></div><div class="cx-sl-ftmetrics"><div class="cx-sl-ftmetric"><span>Pre-sale O / C</span><b>${pct(o.frozen_opportunity_score)} / ${pct(o.frozen_collector_score)}</b></div><div class="cx-sl-ftmetric"><span>Latest low</span><b>${money(o.latest_low_price)}</b></div><div class="cx-sl-ftmetric"><span>Release trough</span><b>${money(o.release_trough_price)}</b></div><div class="cx-sl-ftmetric"><span>Drain from peak</span><b>${pct(o.drain_from_peak_pct)}</b></div></div>${bars(rows)}<div class="cx-sl-ftnote">Peak ${Number(o.listing_peak||0)} listings · rebuild from trough ${pct(o.rebuild_from_trough_pct)} · trough ROI after 25% friction ${pct(o.net_roi_from_trough_pct)}</div></div>`}
async function render(){try{const rs=await rest(`secret_lair_releases?select=release_id&release_name=eq.${encodeURIComponent(LIVE_NAME)}&limit=1`),r=rs?.[0];if(!r)return;const [drops,outcomes,obs]=await Promise.all([rest(`secret_lair_drops?select=drop_id,drop_name&release_id=eq.${r.release_id}&order=created_at.asc`),rest(`secret_lair_forward_test_outcomes?select=*&release_id=eq.${r.release_id}`),rest(`secret_lair_market_observations?select=drop_id,finish,observed_at,listing_count,low_price,market_price&release_id=eq.${r.release_id}&source_market=eq.tcgplayer&observation_type=eq.market_snapshot&order=observed_at.asc&limit=2000`)]);const om=new Map((outcomes||[]).map(x=>[`${x.drop_id}:${x.finish}`,x]));for(const d of drops||[]){const a=article(d.drop_name);if(!a)continue;let box=a.querySelector('.cx-sl-forward');if(!box){box=document.createElement('details');box.className='cx-sl-forward';a.appendChild(box)}const nf=(obs||[]).filter(x=>x.drop_id===d.drop_id&&x.finish==='nonfoil'),fo=(obs||[]).filter(x=>x.drop_id===d.drop_id&&x.finish==='foil');box.innerHTML=`<summary>Forward test · frozen call vs live outcome</summary><div class="cx-sl-ftgrid">${card('Nonfoil',om.get(`${d.drop_id}:nonfoil`),nf)}${card('Foil',om.get(`${d.drop_id}:foil`),fo)}</div>`}}catch{}}
export async function install(){style();await render();document.addEventListener('collectish:intel-changed',()=>setTimeout(()=>void render(),0))}
