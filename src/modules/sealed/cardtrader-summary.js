import store from '../../state/store.js';
import { rest } from '../../core/rest.js';

let loading=false,lastAt=0;
function ensure(){const root=document.getElementById('cxSealed');if(!root)return null;let bar=document.getElementById('cxCt0Summary');if(!bar){bar=document.createElement('div');bar.id='cxCt0Summary';bar.className='cx-sealed-summary';const toolbar=root.querySelector('.cx-sealed-toolbar');if(toolbar)toolbar.after(bar);else root.prepend(bar)}return bar}
async function load(force=false){if(loading||(!force&&Date.now()-lastAt<60000))return;const bar=ensure();if(!bar)return;loading=true;try{const rows=await rest('sealed_product_price_current?select=sealed_uuid,raw_json,captured_at&source=eq.cardtrader&limit=1000').catch(()=>[]);const ct=rows||[],opps=ct.filter(x=>x.raw_json?.ct_zero_opportunity?.eligible===true),strong=opps.filter(x=>x.raw_json?.ct_zero_opportunity?.tier==='strong'),tight=opps.filter(x=>['tightening','strong_tightening'].includes(x.raw_json?.ct_zero_trend?.signal)),stocked=ct.filter(x=>Number(x.raw_json?.ct_zero?.quantity||0)>0),building=ct.filter(x=>(x.raw_json?.ct_zero_trend?.signal||'building_history')==='building_history').length,last=ct.map(x=>x.captured_at).filter(Boolean).sort().at(-1)||null;bar.innerHTML=`<strong>CardTrader Zero:</strong> ${opps.length} opportunities · ${strong.length} strong · ${tight.length} tightening · ${stocked.length} stocked${building?` · ${building} building history`:''}${last?` <span class="cx-sub">· synced ${new Date(last).toLocaleTimeString([], {hour:'numeric',minute:'2-digit'})}</span>`:''} <button type="button" class="cx-refresh" data-ct0-summary-action="opps">View opportunities</button>${tight.length?` <button type="button" class="cx-refresh" data-ct0-summary-action="tight">View tightening</button>`:''}`;lastAt=Date.now()}finally{loading=false}}
function setMode(mode){const select=document.getElementById('cxSealedFilter');if(!select)return;select.value=mode;select.dispatchEvent(new Event('change',{bubbles:true}))}
document.addEventListener('collectish:sealed-rendered',()=>load().catch(()=>{}));document.addEventListener('click',e=>{const b=e.target.closest?.('[data-ct0-summary-action]');if(!b)return;setMode(b.dataset.ct0SummaryAction==='tight'?'ct0tightening':'ct0opps')});window.addEventListener('focus',()=>load(true).catch(()=>{}));window.CollectishCt0Summary={refresh:()=>load(true)};