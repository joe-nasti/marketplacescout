import { rest } from '../../core/rest.js';
const KEY='collectishSealedLanguageFilter';let meta=new Map(),metaLoading=false,lastMeta=0,observedRoot=null,observer=null,scheduled=false;
function classify(sc={}){const lang=String(sc.sealed_language||'English'),mode=String(sc.language_pricing_mode||'exact_or_default_english'),exact=Number(sc.exact_language_coverage_pct||0),fallback=Number(sc.english_fallback_coverage_pct||0);if(mode==='english_equivalent_fallback'||fallback>0)return'fallback';if(lang.toLowerCase()!=='english'&&exact>0)return'nonenglish_exact';return'english_exact'}
async function loadMeta(force=false){if(metaLoading)return;if(!force&&Date.now()-lastMeta<60000&&meta.size)return;metaLoading=true;try{const rows=await rest('sealed_ev_current?select=sealed_uuid,score_components&limit=5000');meta=new Map((rows||[]).map(r=>[String(r.sealed_uuid),r.score_components||{}]));lastMeta=Date.now()}catch(error){console.warn('sealed language metadata unavailable',error)}finally{metaLoading=false}}
function removeOperationalTiles(){const detail=document.getElementById('cxSealedDetail');if(!detail)return;detail.querySelectorAll('.cx-sealed-stat').forEach(tile=>{const label=(tile.querySelector('span')?.textContent||'').trim().toLowerCase();if(label==='lifecycle'||label==='components')tile.remove()})}
function ensureFilter(){const toolbar=document.querySelector('#cxSealed .cx-sealed-toolbar');if(!toolbar)return null;let select=document.getElementById('cxSealedLanguagePricing');if(select)return select;select=document.createElement('select');select.id='cxSealedLanguagePricing';select.innerHTML='<option value="all">All language pricing</option><option value="exclude_fallback">Exclude fallback pricing</option><option value="english_exact">English / exact-default only</option><option value="nonenglish_exact">Non-English exact-language only</option><option value="fallback">English fallback only</option>';select.value=localStorage.getItem(KEY)||'all';select.addEventListener('change',()=>{localStorage.setItem(KEY,select.value);applyFilter()});toolbar.appendChild(select);return select}
function applyFilter(){const select=ensureFilter();if(!select)return;const mode=select.value||'all';let shown=0,hidden=0;document.querySelectorAll('#cxSealedRows [data-deck]').forEach(card=>{const cls=classify(meta.get(String(card.dataset.deck))||{}),ok=mode==='all'||(mode==='exclude_fallback'?cls!=='fallback':cls===mode);card.hidden=!ok;ok?shown++:hidden++});let count=document.getElementById('cxSealedLanguageFilterCount');if(mode==='all'){count?.remove();return}if(!count){count=document.createElement('small');count.id='cxSealedLanguageFilterCount';count.className='cx-sealed-language-filter-count';select.insertAdjacentElement('afterend',count)}count.textContent=`${shown} shown · ${hidden} hidden`}
async function reconcile({forceMeta=false}={}){const root=document.getElementById('cxSealed');if(!root||!root.querySelector('.cx-sealed-toolbar'))return;await loadMeta(forceMeta);ensureFilter();applyFilter();removeOperationalTiles()}
function schedule(options={}){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;reconcile(options).catch(()=>{})})}
function observeSealedRoot(){const root=document.getElementById('cxSealed');if(!root||root===observedRoot)return;observer?.disconnect();observedRoot=root;observer=new MutationObserver(mutations=>{if(mutations.some(m=>m.addedNodes.length||m.removedNodes.length))schedule()});observer.observe(root,{childList:true,subtree:true})}
export function installSealedUiGuardian(){document.addEventListener('collectish:ready',()=>{observeSealedRoot();schedule({forceMeta:true})});document.addEventListener('collectish:lazy-page-loaded',e=>{if(e.detail?.page!=='sealed')return;observeSealedRoot();schedule({forceMeta:true})});document.addEventListener('click',e=>{if(e.target.closest?.('#cxSealedRefresh,[data-cx-page="sealed"]'))setTimeout(()=>schedule({forceMeta:true}),100);else if(e.target.closest?.('#cxSealedRows [data-deck]'))setTimeout(()=>schedule(),0)},true);if(document.readyState!=='loading'){observeSealedRoot();schedule({forceMeta:true})}}
installSealedUiGuardian();export{applyFilter as applySealedLanguageFilter,classify as classifySealedLanguagePricing};
