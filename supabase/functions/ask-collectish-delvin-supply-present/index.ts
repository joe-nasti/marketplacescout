import "jsr:@supabase/functions-js/edge-runtime.d.ts";
const U=(Deno.env.get('SUPABASE_URL')||'').replace(/\/$/,'');
const S=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||'';
const C={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST, OPTIONS'};
const js=(b:any,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...C,'Content-Type':'application/json','Cache-Control':'no-store'}});
const text=(v:any)=>String(v??'').trim();
const sh=()=>({apikey:S,Authorization:`Bearer ${S}`,'Content-Type':'application/json'});
async function parse(r:Response){const raw=await r.text();try{return raw?JSON.parse(raw):null}catch{return raw}}
async function rest(path:string,o:any={}){const r=await fetch(`${U}/rest/v1/${path}`,{method:o.method||'GET',headers:{...sh(),...(o.prefer?{Prefer:o.prefer}:{})},body:o.body===undefined?undefined:JSON.stringify(o.body)}),d:any=await parse(r);if(!r.ok)throw Error(d?.message||`REST ${r.status}`);return d}
async function fn(name:string,body:any){const r=await fetch(`${U}/functions/v1/${name}`,{method:'POST',headers:sh(),body:JSON.stringify(body)}),d:any=await parse(r);if(!r.ok)throw Error(d?.error||`${name} ${r.status}`);return d}
async function rpc(name:string,body:any){return rest(`rpc/${name}`,{method:'POST',body})}
const norm=(v:any)=>text(v).toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
function alias(q:string){for(const p of[/^how(?:'s| is)\s+(?:the\s+)?(?:stock|supply|inventory|liquidity)\s+(?:on|for|of)\s+(.+?)(?:[?.!,]|$)/i,/^(?:what(?:'s| is)|show(?: me)?|give me)\s+(?:the\s+)?(?:stock|supply|inventory|liquidity)\s+(?:on|for|of)\s+(.+?)(?:[?.!,]|$)/i,/\b(?:stock|supply|inventory|liquidity)\s+(?:on|for|of)\s+(.+?)(?:[?.!,]|$)/i]){const m=text(q).match(p);if(m?.[1])return m[1].trim()}return''}
const nfmt=(v:any)=>Number(v||0).toLocaleString('en-US');
const pct=(v:any)=>Number.isFinite(Number(v))?`${Number(v).toFixed(1)}%`:'—';
const ageMin=(v:any)=>{const t=Date.parse(text(v));return Number.isFinite(t)?Math.max(0,(Date.now()-t)/60000):null};
function finish(v:any){const s=text(v).toUpperCase();return s.includes('FOIL')&&!s.includes('NON FOIL')?'FOIL':'NON FOIL'}
function labelFor(productId:any,finishScope:any,targets:any[]){const x=(targets||[]).find((r:any)=>text(r?.product_id)===text(productId)&&finish(r?.printing)===finishScope)||(targets||[]).find((r:any)=>text(r?.product_id)===text(productId));return [x?.set_code,x?.collector_number?`#${x.collector_number}`:null,finishScope==='FOIL'?'foil':'nonfoil'].filter(Boolean).join(' ')||`${productId} ${finishScope.toLowerCase()}`}
function classificationSummary(v:any){const s=text(v).toUpperCase();if(s==='DEEP')return'Plenty available across the measured NM/LP family.';if(s==='MODERATE')return'Supply looks healthy overall, with some tighter variants.';if(s==='THIN'||s==='VERY_THIN')return'The measured NM/LP family is genuinely tight.';return'Current family depth is incomplete.'}
async function cardKingdomDepth(skus:string[]){if(!skus.length)return{quantity:0,mapped_skus:0,observed_at:null};const ids=await rest(`vendor_item_identities?source=eq.cardkingdom&tcgplayer_sku_id=in.(${skus.join(',')})&select=source_item_key,tcgplayer_sku_id`).catch(()=>[]);const keys=[...new Set((ids||[]).map((x:any)=>text(x.source_item_key)).filter(Boolean))];const mappedSkus=new Set((ids||[]).map((x:any)=>text(x.tcgplayer_sku_id)).filter(Boolean));if(!keys.length)return{quantity:0,mapped_skus:mappedSkus.size,observed_at:null};const rows=await rest(`vendor_depth_current?source=eq.cardkingdom&lane=eq.retail_supply&source_item_key=in.(${keys.map(k=>encodeURIComponent(k)).join(',')})&select=source_item_key,quantity,condition,language,observed_at`).catch(()=>[]);const eligible=(rows||[]).filter((r:any)=>['NM','EX'].includes(text(r.condition).toUpperCase())&&['','EN','ENGLISH'].includes(text(r.language).toUpperCase()));let newest:string|null=null;for(const r of eligible)if(!newest||Date.parse(r.observed_at)>Date.parse(newest))newest=r.observed_at;return{quantity:eligible.reduce((n:number,r:any)=>n+Number(r.quantity||0),0),mapped_skus:mappedSkus.size,observed_at:newest}}

Deno.serve(async req=>{
 if(req.method==='OPTIONS')return new Response('ok',{headers:C});if(req.method!=='POST')return js({error:'POST required'},405);if(!S)return js({error:'Service role unavailable'},500);
 const body=await req.json().catch(()=>({})),q=text(body?.question||body?.message),name=alias(q);if(!name)return js({handled:false,reason:'not_supply_question'});
 let identity:any;try{identity=await fn('ask-collectish-identity-recovery',{question:q,message:q,context:{}})}catch(e){return js({handled:false,reason:'identity_recovery_failed',error:String((e as Error)?.message||e)})}
 const supply=identity?.market_supply,targets=Array.isArray(supply?.exact_skus)?supply.exact_skus:[];
 if(supply?.scope!=='CARD_FAMILY_NM_LP'||!targets.length)return js({handled:false,reason:'no_nm_lp_family_supply'});
 const skus=[...new Set(targets.map((x:any)=>text(x?.sku_id)).filter((x:string)=>/^\d+$/.test(x)))];if(!skus.length)return js({handled:false,reason:'no_exact_family_skus'});
 const [concentration,trend,ck]=await Promise.all([
   rpc('ask_collectish_family_supply_concentration_v1',{p_sku_ids:skus}).catch(()=>null),
   rpc('ask_collectish_family_supply_trend_v1',{p_sku_ids:skus,p_days:90}).catch(()=>null),
   cardKingdomDepth(skus)
 ]);
 const total=supply?.combined||{},cardName=text(targets[0]?.card_name||identity?.alias||name),classification=text(supply?.classification||total?.classification||'UNPROVEN').toUpperCase(),summaryBase=classificationSummary(classification),mpQty=Number(total?.manapool_retail_quantity||0),tcg=Number(total?.unit_count||0),listings=Number(total?.listing_count||0),direct=Number(total?.direct_unit_count||0),nonDirect=Number(total?.non_direct_unit_count||0);
 const rows=Array.isArray(concentration?.printing_rows)?concentration.printing_rows:[],deep=concentration?.deepest_printing||null,tight=concentration?.tightest_printing||null,top1=Number(concentration?.top1_supply_share_pct),top3=Number(concentration?.top3_supply_share_pct),conc=text(concentration?.concentration_classification||'');
 const deepLabel=deep?labelFor(deep.product_id,deep.finish,targets):null,tightLabel=tight?labelFor(tight.product_id,tight.finish,targets):null;
 const summaryParts=[`${classification} · ${summaryBase}`];if(Number.isFinite(top1)&&deepLabel)summaryParts.push(`${pct(top1)} of TCG supply is in ${deepLabel}`);if(tight&&tightLabel&&['THIN','VERY_THIN'].includes(text(tight.supply_classification)))summaryParts.push(`${tightLabel} is ${text(tight.supply_classification)} (${nfmt(tight.unit_count)} units)`);const summary=summaryParts.join(' · ');
 const trendLabel=text(trend?.trend||'UNPROVEN'),trendPoints=Number(trend?.complete_observation_points||0),trendText=trendLabel==='UNPROVEN'?`No historical direction yet — ${trendPoints} complete family observation${trendPoints===1?'':'s'}.`:`${nfmt(trend?.first_units)} → ${nfmt(trend?.last_units)} TCG units (${Number(trend?.unit_change_pct)>=0?'+':''}${Number(trend?.unit_change_pct||0).toFixed(1)}%) across ${nfmt(trend?.observed_span_days)} observed days · ${trendLabel.replaceAll('_',' ')}`;
 const variantRows=rows.slice(0,10).map((r:any)=>({title:labelFor(r.product_id,r.finish,targets),subtitle:`${text(r.supply_classification).replaceAll('_',' ')} · ${pct(r.supply_share_pct)} of family TCG supply`,badges:[text(r.supply_classification)],metrics:[{label:'TCG',value:r.unit_count,display:nfmt(r.unit_count)},{label:'Listings',value:r.listing_count,display:nfmt(r.listing_count)},{label:'Direct',value:r.direct_unit_count,display:nfmt(r.direct_unit_count)}],raw:r}));
 const cache=supply?.cache||{},tcgHits=Number(cache?.tcgplayer?.hits||0),mpHits=Number(cache?.manapool?.hits||0),targetCount=Number(supply?.coverage?.target_sku_count||targets.length),newest=targets.reduce((a:any,r:any)=>!a||Date.parse(r.observed_at)>Date.parse(a)?r.observed_at:a,null),age=newest?ageMin(newest):null,ckAge=ck.observed_at?ageMin(ck.observed_at):null;
 const response=`**${cardName} — Stock**\n**${classification}** · ${summaryBase}\n\nTCGplayer: **${nfmt(tcg)}** units across **${nfmt(listings)}** listings\n↳ Direct **${nfmt(direct)}** · Non-Direct **${nfmt(nonDirect)}**\nManaPool: **${nfmt(mpQty)}** copies\nCard Kingdom mapped NM/LP: **${nfmt(ck.quantity)}** copies\n\n${deepLabel&&Number.isFinite(top1)?`Supply concentration: **${pct(top1)}** in ${deepLabel}; top 3 = **${pct(top3)}**.\n`:''}${tight&&tightLabel?`Tightest printing: **${tightLabel}** — ${text(tight.supply_classification)} (${nfmt(tight.unit_count)} units).\n`:''}History: ${trendText}`;
 const sections:any[]=[];
 if(concentration?.available)sections.push({heading:'Supply concentration',kind:'text',text:`${conc.replaceAll('_',' ')} · largest printing ${pct(top1)} · top 3 ${pct(top3)}${Number.isFinite(Number(concentration?.hhi))?` · HHI ${nfmt(concentration.hhi)}`:''}. A scarce premium printing does not make the whole card family thin.`});
 if(variantRows.length)sections.push({heading:'Printing depth',kind:'ranked_rows',rows:variantRows});
 sections.push({heading:'Observed trend',kind:'text',text:trendText});
 const foot=[`English NM/LP Oracle-family scope`,`TCGplayer/ManaPool cache 30m`,`CK baseline 12h`,`${supply?.coverage?.complete_sku_count||0}/${targetCount} TCG SKUs complete`];if(tcgHits||mpHits)foot.push(`cache reused ${tcgHits} TCG / ${mpHits} MP SKU observations`);if(age!=null)foot.push(`TCG ${age<1?'under 1m':Math.round(age)+'m'} old`);if(ckAge!=null)foot.push(`CK ${Math.round(ckAge/60)}h old`);foot.push(`CK mapping ${ck.mapped_skus}/${skus.length} SKUs`);
 const presentation={version:2,type:'card_family_supply',title:`${cardName} — Stock`,summary,badges:[classification,...(conc?[conc.replaceAll('_',' ')]:[])],metrics:[{label:'TCGplayer',value:tcg,display:`${nfmt(tcg)} units · ${nfmt(listings)} listings`},{label:'Direct',value:direct,display:nfmt(direct)},{label:'Non-Direct',value:nonDirect,display:nfmt(nonDirect)},{label:'ManaPool',value:mpQty,display:nfmt(mpQty)},{label:'CK mapped NM/LP',value:ck.quantity,display:nfmt(ck.quantity)}],sections,footnote:foot.join(' · '),actions:[]};
 return js({handled:true,route:'card_family_supply',response,presentation,data:{card_name:cardName,scope:'CARD_FAMILY_NM_LP',market_supply:supply,concentration,trend,cardkingdom:ck,sku_ids:skus}});
});
