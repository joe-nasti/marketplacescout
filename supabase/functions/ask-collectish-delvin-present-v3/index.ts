// Immutable release endpoint. Supabase bundles this dependency at deployment,
// so later deployments to the compatibility v2 slug cannot replace v3.
import "../ask-collectish-delvin-present-v2/index.ts";
