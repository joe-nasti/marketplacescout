# Shared Ask deterministic routes

Price-history routing should resolve the user's full natural-language question through the shared card lookup before relying on stripped aliases. The lookup owns conversational-prefix and set/collector normalization so Discord, web Ask, and future clients converge on the same exact printing identity.
