const URL=(process.env.SUPABASE_URL||'').replace(/\/$/,'');
const KEY=process.env.SUPABASE_SERVICE_ROLE_KEY||'';
const SEARCH='https://mp-search-api.tcgplayer.com';
const RELEASE_FROM=process.env.PRECON_RELEASE_FROM||'2025-01-01';
if(!URL||!KEY)throw new Error('Missing Supabase credentials');
const H={apikey:KEY,Authorization:`Bearer ${KEY}`,'Content-Type':'application/json'};
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
async function sb(path,{method='GET',body,prefer}={}){const r=await fetch(`${URL}/rest/v1/${path}`,{method,headers:{...H,...(prefer?{Prefer:prefer}:{})},body:body===undefined?undefined:JSON.stringify(body)});const t=await r.text();let d=null;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok)throw new Error(`${r.status} ${path}: ${typeof d==='string'?d:JSON.stringify(d)}`);return d}
function jsonish(v){if(v&&typeof v==='object')return v;if(typeof v==='string'){try{return JSON.parse(v)}catch{}}return {}}
async function search(q){const body={algorithm:'salesrel',from:0,size:64,filters:{term:{productLineName:['magic'],productTypeName:['Sealed Products']},range:{},match:{}},context:{shippingCountry:'US',userProfile:{productLineAffinity:'Magic: The Gathering'}},settings:{useFuzzySearch:true,didYouMean:{}},sort:{}};const r=await fetch(`${SEARCH}/v1/search/request?q=${encodeURIComponent(q)}&isList=true`,{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify(body)});const t=await r.text();if(!r.ok)throw new Error(`TCG search ${r.status}: ${t.slice(0,240)}`);return JSON.parse(t)}
function norm(s){return String(s||'').toLowerCase().replace(/collector'?s edition/g,' collectors edition ').replace(/commander deck/g,' ').replace(/magic the gathering/g,' ').replace(/[^a-z0-9]+/g,' ').trim()}
function scoreName(expected,actual){const a=new Set(norm(expected).split(' ').filter(Boolean)),b=new Set(norm(actual).split(' ').filter(Boolean));let hit=0;for(const x of a)if(b.has(x))hit++;return a.size?hit/a.size:0}
function rows(j){return j?.results?.[0]?.results||[]}
function parseIdFromUrl(u){try{const x=new URL(u);const m=x.pathname.match(/\/product\/(\d+)/i);return m?.[1]||x.searchParams.get('productId')||x.searchParams.get('productid')||null}catch{return null}}
async function resolvePurchaseId(raw){const urls=jsonish(raw),link=urls.tcgplayer;if(!link)return null;try{const r=await fetch(link,{redirect:'follow',headers:{'User-Agent':'Collectish-Sealed/1.2'}});return parseIdFromUrl(r.url)}catch{return null}}
const BAD_STANDALONE=/\b(kit|bundle|display|case|case of|set of|complete set|all (four|five|4|5)|4[- ]deck|5[- ]deck)\b/i;
function isCollector(s){return /collector'?s edition/i.test(String(s||''))}
function compatible(expected,actual){
  const e=String(expected||''),a=String(actual||'');
  if(isCollector(e)!==isCollector(a))return false;
  if(!BAD_STANDALONE.test(e)&&BAD_STANDALONE.test(a))return false;
  return true;
}
function choose(j,expected,exactId){const rr=rows(j);if(exactId){const x=rr.find(p=>String(p?.productId)===String(exactId)&&compatible(expected,p?.productName));if(x)return x}let best=null,bestScore=-1;for(const p of rr){if(!p?.productId||!compatible(expected,p.productName))continue;const sc=scoreName(expected,p.productName);if(sc>bestScore){bestScore=sc;best=p}}return bestScore>=.48?best:null}
function shortName(s){return String(s||'').replace(/^.*?Commander Deck\s+/i,'').replace(/^Secret Lair Commander Deck\s+/i,'').trim()}
function decode(v){if(Array.isArray(v))return v.flatMap(x=>decode(x));if(typeof v==='string'){try{const x=JSON.parse(v);return Array.isArray(x)?x:[v]}catch{return [v]}}return []}
const decks=await sb(`mtgjson_decks?select=deck_key,name,release_date,sealed_product_uuids&deck_type=eq.${encodeURIComponent('Commander Deck')}&release_date=gte.${encodeURIComponent(RELEASE_FROM)}&order=release_date.desc&limit=500`)||[];
let written=0,matched=0,failed=0,redirectResolved=0,rejected=0;
for(const d of decks){const uuids=decode(d.sealed_product_uuids).filter(x=>/^[0-9a-f-]{36}$/i.test(String(x)));if(!uuids.length)continue;const products=await sb(`mtgjson_sealed_products?select=uuid,name,set_code,tcgplayer_product_id,purchase_urls&uuid=in.(${uuids.map(encodeURIComponent).join(',')})`);for(const sp of products||[]){try{let exactId=sp.tcgplayer_product_id||await resolvePurchaseId(sp.purchase_urls);if(exactId)redirectResolved++;const queries=[shortName(sp.name||d.name),sp.name||d.name,d.name].filter((x,i,a)=>x&&a.indexOf(x)===i);let p=null;for(const q of queries){const j=await search(q);p=choose(j,sp.name||d.name,exactId);if(p)break;await sleep(120)}if(!p){rejected++;console.warn(`No safe sealed TCG match: ${sp.name} exact=${exactId||'none'}`);continue}matched++;const listing=(p.listings||[])[0]||{};const row={sealed_uuid:sp.uuid,source:'tcgplayer_public',product_id:String(p.productId),product_name:p.productName||sp.name,market_price:p.marketPrice==null?null:Number(p.marketPrice),low_price:p.lowestPrice==null?null:Number(p.lowestPrice),low_with_shipping:p.lowestPriceWithShipping==null?null:Number(p.lowestPriceWithShipping),total_listings:p.totalListings==null?null:Number(p.totalListings),captured_at:new Date().toISOString(),raw_json:{setName:p.setName,productTypeName:p.productTypeName,listingPrice:listing.price??null,query:sp.name||d.name,mtgjsonPurchaseId:exactId||null,matchGuard:'standalone-v2'}};await sb('sealed_product_price_current?on_conflict=sealed_uuid,source',{method:'POST',body:[row],prefer:'resolution=merge-duplicates,return=minimal'});written++;await sb(`mtgjson_sealed_products?uuid=eq.${encodeURIComponent(sp.uuid)}`,{method:'PATCH',body:{tcgplayer_product_id:String(p.productId)},prefer:'return=minimal'});console.log(`${sp.name}: product ${p.productId} market ${p.marketPrice??'—'} low ${p.lowestPrice??'—'} exact ${exactId||'—'}`);await sleep(180)}catch(e){failed++;console.error(`${sp.name}: ${e.message}`)}}}
console.log(JSON.stringify({decks:decks.length,matched,written,failed,rejected,redirectResolved,releaseFrom:RELEASE_FROM,at:new Date().toISOString()}));if(failed)process.exitCode=1;
