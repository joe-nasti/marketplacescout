const DISCORD_API='https://discord.com/api/v10';const enc=new TextEncoder(),dec=new TextDecoder();const base=env=>String(env.SUPABASE_URL||'').replace(/\/$/,'');
function json(b,s=200){return new Response(JSON.stringify(b),{status:s,headers:{'Content-Type':'application/json','Cache-Control':'no-store'}})}
function hex(v,n){const h=String(v||'').trim();if(!new RegExp(`^[0-9a-f]{${n*2}}$`,'i').test(h))return null;const o=new Uint8Array(n);for(let i=0;i<n;i++)o[i]=parseInt(h.slice(i*2,i*2+2),16);return o}
async function verify(req,raw,keyHex){const sig=hex(req.headers.get('x-signature-ed25519'),64),ts=req.headers.get('x-signature-timestamp')||'',pk=hex(keyHex,32);if(!sig||!ts||!pk)return false;const a=enc.encode(ts),b=new Uint8Array(raw),m=new Uint8Array(a.length+b.length);m.set(a);m.set(b,a.length);const k=await crypto.subtle.importKey('raw',pk,{name:'Ed25519'},false,['verify']);return crypto.subtle.verify({name:'Ed25519'},k,sig,m)}
function qFrom(x){const o=(x?.data?.options||[]).find(v=>String(v?.name||'').toLowerCase()==='question');return String(o?.value||'').trim()}
async function route(env,q){const r=await fetch(`${base(env)}/functions/v1/ask-collectish-delvin-route`,{method:'POST',headers:{apikey:env.SUPABASE_SERVICE_ROLE_KEY,Authorization:`Bearer ${env.SUPABASE_SERVICE_ROLE_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({question:q,client:'discord'})});if(!r.ok)return null;return r.json()}
function cleanMarkdown(s){return String(s||'').replace(/^##\s+/gm,'').replace(/^###\s+/gm,'**').replace(/^(\*\*[^\n]+)$/gm,'$1**')}
const colors={BUY:0x2da44e,WATCH:0xbf8700,PASS:0xcf222e};
const usd=v=>Number.isFinite(Number(v))?`$${Number(v).toFixed(2)}`:'—';
async function edit(i,env,d){const surface=(d.surfaces||[]).find(x=>x?.type==='sealed_purchase_decision'),canonical=surface?.response||d.response;let description=surface?surface.action:cleanMarkdown(canonical);if(description.length>3900)description=description.slice(0,3897)+'…';const title=surface?`${surface.title} — ${surface.verdict}`:`Delvin · ${String(d.route||'market intelligence').replaceAll('_',' ')}`,fields=surface?[{name:'Market',value:usd(surface.market_price),inline:true},{name:'MSRP',value:usd(surface.msrp),inline:true},{name:'Modeled EV',value:`${usd(surface.gross_mean_ev)} gross\n${usd(surface.net_mean_ev_after_fees)} net`,inline:true},{name:'Market activity',value:`${surface.units_sold} packs · ${surface.transactions} transactions\n${usd(surface.low_sale)}–${usd(surface.high_sale)} · ${Number(surface.price_multiple_vs_msrp).toFixed(1)}× MSRP`,inline:false},{name:`Why ${surface.verdict}`,value:(surface.why||[]).map(x=>`• ${x}`).join('\n'),inline:false},{name:'What could change it',value:(surface.changes||[]).map(x=>`• ${x}`).join('\n'),inline:false},{name:'Buy zone',value:surface.buy_zone,inline:true},{name:'Recheck',value:surface.recheck,inline:true}]:undefined,embed={title,description,...(fields?{fields}:{}),...(surface?.verdict?{color:colors[surface.verdict]||0x5865f2}:{}),...(surface?.image_url?{thumbnail:{url:surface.image_url}}:{})};const r=await fetch(`${DISCORD_API}/webhooks/${i.application_id||env.DISCORD_APPLICATION_ID}/${i.token}/messages/@original`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({content:'',embeds:[embed],components:[],allowed_mentions:{parse:[]}})});if(!r.ok)throw Error(`Discord edit HTTP ${r.status}`)}
export function isQueuedSharedQuestion(q){const s=String(q||'').toLowerCase();return /\bzeta\b/.test(s)&&/\b(?:booster|boosters|pack|packs|aftermarket|secondary|buy|bought|missed out|missed)\b/.test(s)}
async function delivery(env,id,patch){if(!id)return;await fetch(`${base(env)}/rest/v1/discord_ask_deliveries?interaction_id=eq.${encodeURIComponent(id)}`,{method:'PATCH',headers:{apikey:env.SUPABASE_SERVICE_ROLE_KEY,Authorization:`Bearer ${env.SUPABASE_SERVICE_ROLE_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({...patch,updated_at:new Date().toISOString()})}).catch(()=>null)}
export async function deliverQueuedSharedQuestion(env,job,message){const d=await route(env,job.question).catch(()=>null);if(!d?.handled||!d?.response)return false;try{await edit({application_id:job.application_id,token:job.interaction_token},env,d);await delivery(env,job.interaction_id,{response_text:String(d.response).slice(0,1900),status:'completed',completed_at:new Date().toISOString(),error_text:null});message.ack();return true}catch(error){const detail=String(error?.message||error).slice(0,500);await delivery(env,job.interaction_id,{status:'failed',completed_at:new Date().toISOString(),error_text:detail});throw error}}
export async function maybeHandleSharedDelvinRoute(request,env,ctx){if(request.method!=='POST')return null;let raw,p;try{raw=await request.clone().arrayBuffer();p=JSON.parse(dec.decode(raw))}catch{return null}if(p?.type!==2||String(p?.data?.name||'').toLowerCase()!=='ask')return null;if(!await verify(request,raw,env.DISCORD_PUBLIC_KEY))return new Response('invalid request signature',{status:401});const q=qFrom(p);if(!q)return null;if(isQueuedSharedQuestion(q))return null;const d=await route(env,q).catch(()=>null);if(!d?.handled||!d?.response)return null;ctx?.waitUntil?.(edit(p,env,d).catch(e=>console.warn('shared Delvin Discord edit failed',String(e?.message||e))));return json({type:5,data:{}})}
